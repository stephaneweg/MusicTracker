# Rapport quotidien — 2026-07-28

## Run annulé : travail en cours détecté

Ce run (déclenché manuellement en test de la tâche planifiée `musictracker-daily-feature`) a été annulé avant toute implémentation, conformément à la règle de sécurité du prompt : le repo n'était pas propre au moment du run.

`git status` montrait les changements non commités suivants (probablement une session de travail en cours de l'utilisateur, notamment un dialogue de mixer) :

- Modifiés : `MusicTracker/Engine/Timeline/LookaheadBuffer.cs`, `MusicTracker/Engine/Timeline/TimelinePlayer.cs`, `MusicTracker/Engine/Timeline/TimelineProject.cs`, `MusicTracker/Engine/WaveExporter.cs`, `MusicTracker/Localization/lang.fr.json`, `MusicTracker/MusicTracker.csproj`, `MusicTracker/Screens/TimelineScreen.xaml`, `MusicTracker/Screens/TimelineScreen.xaml.cs`
- Nouveau (non suivi) : `MusicTracker/Dialogs/MixerDialog.cs`

Aucun fichier de code n'a été touché par ce run. Seul ce rapport a été ajouté et commité (aucun autre fichier n'a été mis en staging).

## Idées repérées pour un prochain run (une fois le repo libre)

Non explorées en détail ce run puisque l'analyse s'est arrêtée à l'étape 1, mais à considérer en priorité au prochain passage propre du repo :

- Reprendre `KANBAN.md` et le backlog "reste" de la roadmap 2026-07 (swing, exports batterie mscx/PDF direct/MusicXML, enregistrement tap-tempo violon, filet basse compos IA) pour choisir la prochaine petite feature.
- Vérifier si le `MixerDialog.cs` en cours (non commité) est lié à l'item "pan/mixer" proposé dans la roadmap — si oui, un futur run pourra le compléter au lieu de partir sur autre chose.

## Run #2 (même jour) : à nouveau annulé

Second run manuel déclenché le même jour. Entre-temps, le travail mixer a bien été commité (`9acfce4 Mixer + panoramique stéréo : dialogue XAML, live par piste`), mais deux commits `AutoTest` sont apparus (`80217d0`, `88ea5c0`) et `git status` montre à nouveau des changements non commités :

- Modifiés : `AutoTest/Program.cs`, `AutoTest/results.json`

Cela ressemble à un processus (test automatisé ?) qui écrit dans le repo en parallèle. Par prudence, ce run est annulé lui aussi sans toucher à ces fichiers. Si `AutoTest/` est un outil qui tourne en continu et modifie ces fichiers régulièrement, il faudra soit l'ajouter au `.gitignore`, soit adapter la règle "repo propre" de la tâche planifiée pour ignorer ce dossier spécifiquement — à décider avec l'utilisateur plutôt que de le faire unilatéralement.

## Prochaine exécution

Le prochain run automatique aura lieu à 20h (heure locale), sous réserve que le repo soit propre à ce moment-là.

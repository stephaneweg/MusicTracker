<Window x:Class="MusicTracker.Dialogs.VstPluginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ctrl="clr-namespace:MusicTracker.Controls"
        xmlns:loc="clr-namespace:MusicTracker.Localization"
        mc:Ignorable="d" Foreground="{StaticResource CommonForeground}"
        Title="VST"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource CommonBackground}"
        ShowInTaskbar="False" Closing="Window_Closing">
    <!-- Chrome Windows standard (pas de AllowsTransparency=True → incompatible avec HwndHost et son
         HWND natif : le plugin peindrait bien mais WPF afficherait du noir par-dessus, bug airspace).
         La fenêtre = juste un conteneur pour le HWND du plugin, titre + bouton close = ceux de Windows. -->
    <Border x:Name="hostBorder" Padding="0" Background="#111"/>
</Window>

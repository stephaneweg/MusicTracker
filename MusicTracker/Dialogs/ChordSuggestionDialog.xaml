<Window x:Class="MusicTracker.Dialogs.ChordSuggestionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ctl="clr-namespace:MusicTracker.Controls"
        Title="Suite d'accord" SizeToContent="WidthAndHeight" ResizeMode="NoResize" Loaded="Window_Loaded"
        WindowStartupLocation="CenterOwner" WindowStyle="None" AllowsTransparency="True" Background="Transparent">
    <Border Background="{StaticResource CommonBackground}" BorderBrush="{StaticResource OutlineColorBrush}" BorderThickness="3" CornerRadius="8">
        <StackPanel Margin="10,10">
            <StackPanel Background="{StaticResource CommonBackground}"  ctl:WindowDrag.Enable="True"  Orientation="Horizontal" Margin="0,0,0,2" HorizontalAlignment="Stretch" >
                <TextBlock Text="🎼" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="{StaticResource CommonForeground}" />
                <TextBlock Text="{Binding Title, RelativeSource={RelativeSource AncestorType={x:Type Window}}}" Foreground="{StaticResource CommonForeground}" FontSize="16" FontWeight="Bold" VerticalAlignment="Center"/>
            </StackPanel>
            <TextBlock Text="Suites classées selon les accords précédents, la mesure et l'humeur. ★ = recommandé. Survole pour écouter."
                       Foreground="#888888" FontSize="11" TextWrapping="Wrap" Margin="0,0,0,6" MaxWidth="540"/>
            <Border HorizontalAlignment="Stretch" Height="350" Background="#333333" CornerRadius="5" BorderThickness="1" BorderBrush="{StaticResource OutlineColorBrush}">
                <Grid x:Name="canvas" HorizontalAlignment="Stretch" Height="350">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                </Grid>
            </Border>
            <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                <TextBlock Text="Humeur :" Foreground="#AAAAAA" FontSize="11" VerticalAlignment="Center" Margin="0,0,6,0"/>
                <ComboBox x:Name="cboMood" Width="120" HorizontalAlignment="Left" SelectionChanged="Combo_Changed"/>
                <TextBlock Text="Couleur :" Foreground="#AAAAAA" FontSize="11" VerticalAlignment="Center" Margin="14,0,6,0"/>
                <ComboBox x:Name="cboColour" Width="120" HorizontalAlignment="Left" SelectionChanged="Combo_Changed"/>
                <TextBlock Text="Suspendu :" Foreground="#AAAAAA" FontSize="11" VerticalAlignment="Center" Margin="10,0,6,0"/>
                <ComboBox x:Name="cboSusp" Width="80" HorizontalAlignment="Left" SelectionChanged="Combo_Changed"/>
                <TextBlock Text="Maj/min :" Foreground="#AAAAAA" FontSize="11" VerticalAlignment="Center" Margin="10,0,6,0"/>
                <ComboBox x:Name="cboMode" Width="80" HorizontalAlignment="Left" SelectionChanged="Combo_Changed"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                <Button Content="Annuler" Style="{StaticResource cancelButton}" Padding="16,4" Cursor="Hand" IsCancel="True" Click="Cancel_Click"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>

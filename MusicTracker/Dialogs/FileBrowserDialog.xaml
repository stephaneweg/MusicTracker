<Window x:Class="MusicTracker.Dialogs.FileBrowserDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ctl="clr-namespace:MusicTracker.Controls"
        xmlns:loc="clr-namespace:MusicTracker.Localization"
        Title="{loc:Tr 'OuvrirUnFichier'}" Width="800" Height="540" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" WindowStyle="None" AllowsTransparency="True" Background="Transparent">
    <Window.Resources>
        <!-- A place button in the left sidebar (Récents, Bureau, lecteurs…). -->
        <Style x:Key="PlaceBtn" TargetType="Button">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Foreground" Value="{StaticResource CommonForeground}"/>
            <Setter Property="Padding" Value="10,7"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" Background="Transparent" CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Background" Value="{StaticResource SurfaceHoverBackground}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- A small square icon button (Précédent / Remonter). -->
        <Style x:Key="NavBtn" TargetType="Button">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Foreground" Value="{StaticResource CommonForeground}"/>
            <Setter Property="FontFamily" Value="Segoe UI Symbol"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" Background="{StaticResource LightBackground}" CornerRadius="5"
                                BorderBrush="{StaticResource SubtleBorderBrush}" BorderThickness="1">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Background" Value="{StaticResource SurfaceHoverBackground}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- File / folder list: GridView triable (Nom / Extension / Modifié le). -->
        <Style x:Key="EntryList" TargetType="ListView">
            <Setter Property="Background" Value="{StaticResource DarkBackground}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="{StaticResource CommonForeground}"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
        </Style>
        <Style x:Key="EntryItem" TargetType="ListViewItem">
            <Setter Property="Padding" Value="4,3"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Foreground" Value="{StaticResource CommonForeground}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border x:Name="b" Background="Transparent" Padding="{TemplateBinding Padding}">
                            <GridViewRowPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Background" Value="{StaticResource SurfaceHoverBackground}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="b" Property="Background" Value="{StaticResource AccentDeepBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- Column header — cliquable, sombre, avec indicateur de tri via Tag. -->
        <Style x:Key="EntryHeader" TargetType="GridViewColumnHeader">
            <Setter Property="Background" Value="{StaticResource LightBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource CommonForeground}"/>
            <Setter Property="BorderBrush" Value="{StaticResource SubtleBorderBrush}"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource CommonBackground}" BorderBrush="{StaticResource OutlineColorBrush}" BorderThickness="3" CornerRadius="8">
        <Grid Margin="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title bar (drag) -->
            <Grid Grid.Row="0" ctl:WindowDrag.Enable="True" Background="{StaticResource CommonBackground}" Margin="0">
                <StackPanel Orientation="Horizontal" Margin="18,14,0,10">
                    <TextBlock x:Name="txtIcon" Text="📂" FontSize="15" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock x:Name="txtTitle" Text="{loc:Tr 'OuvrirUnFichier'}" Foreground="{StaticResource CommonForeground}" FontSize="15" FontWeight="Bold" VerticalAlignment="Center"/>
                </StackPanel>
                <Button Content="✕" Click="btnClose_Click" HorizontalAlignment="Right" VerticalAlignment="Top" Width="34" Height="30"
                        Cursor="Hand" Foreground="{StaticResource SecondaryForeground}" Background="Transparent" BorderThickness="0" FontSize="13"/>
            </Grid>

            <!-- Path bar -->
            <Grid Grid.Row="1" Margin="16,2,16,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" x:Name="btnBack" Style="{StaticResource NavBtn}" Content="◀" Margin="0,0,6,0" Click="btnBack_Click" ToolTip="{loc:Tr 'Precedent'}"/>
                <Button Grid.Column="1" x:Name="btnUp" Style="{StaticResource NavBtn}" Content="▲" Margin="0,0,8,0" Click="btnUp_Click" ToolTip="{loc:Tr 'DossierParent'}"/>
                <TextBox Grid.Column="2" x:Name="txtPath" VerticalContentAlignment="Center" Padding="8,0" KeyDown="txtPath_KeyDown"
                         Background="{StaticResource LightBackground}" Foreground="{StaticResource CommonForeground}"
                         BorderBrush="{StaticResource SubtleBorderBrush}"/>
            </Grid>

            <!-- Sidebar + list -->
            <Grid Grid.Row="2" Margin="16,0,16,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="176"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="{StaticResource DarkBackground}" CornerRadius="6" Padding="6" Margin="0,0,10,0">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <StackPanel x:Name="placesPanel"/>
                    </ScrollViewer>
                </Border>

                <Border Grid.Column="1" Background="{StaticResource DarkBackground}" CornerRadius="6">
                    <Grid>
                        <ListView x:Name="listFiles" Style="{StaticResource EntryList}" ItemContainerStyle="{StaticResource EntryItem}"
                                  SelectionChanged="listFiles_SelectionChanged" MouseDoubleClick="listFiles_DoubleClick" Padding="0"
                                  GridViewColumnHeader.Click="ListHeader_Click">
                            <ListView.View>
                                <GridView>
                                    <GridView.ColumnHeaderContainerStyle>
                                        <StaticResource ResourceKey="EntryHeader"/>
                                    </GridView.ColumnHeaderContainerStyle>
                                    <GridViewColumn Header="{loc:Tr 'Nom'}" Width="380">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="{Binding Icon}" FontSize="14" Margin="0,0,9,0" VerticalAlignment="Center"/>
                                                    <TextBlock Text="{Binding Name}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                                </StackPanel>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                    <GridViewColumn Header="{loc:Tr 'Extension'}" Width="80">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Ext}" Foreground="{StaticResource SecondaryForeground}" FontSize="11"/>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                    <GridViewColumn Header="{loc:Tr 'ModifieLe'}" Width="140">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding ModifiedDisplay}" Foreground="{StaticResource SecondaryForeground}" FontSize="11"/>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                </GridView>
                            </ListView.View>
                        </ListView>
                        <TextBlock x:Name="txtEmpty" Text="{loc:Tr 'DossierVide'}" Foreground="{StaticResource SecondaryForeground}"
                                   HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed"/>
                    </Grid>
                </Border>
            </Grid>

            <!-- Bottom bar -->
            <Grid Grid.Row="3" Margin="16,12,16,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="{loc:Tr 'Nom'}" Foreground="{StaticResource CommonForeground}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox Grid.Row="0" Grid.Column="1" x:Name="txtFileName" VerticalContentAlignment="Center" Padding="8,4" KeyDown="txtFileName_KeyDown"
                         Background="{StaticResource LightBackground}" Foreground="{StaticResource CommonForeground}" BorderBrush="{StaticResource SubtleBorderBrush}"/>
                <ComboBox Grid.Row="0" Grid.Column="2" x:Name="cboFilter" MinWidth="220" Margin="10,0,0,0" VerticalAlignment="Center"
                          SelectionChanged="cboFilter_SelectionChanged"/>

                <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
                    <Button Content="{loc:Tr 'Annuler'}" Style="{StaticResource cancelButton}" Click="btnCancel_Click" Padding="18,5" Margin="0,0,8,0" Cursor="Hand"/>
                    <Button x:Name="btnAccept" Content="{loc:Tr 'Ouvrir'}" Style="{StaticResource okButton}" Click="btnAccept_Click" Padding="22,5" Cursor="Hand"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
